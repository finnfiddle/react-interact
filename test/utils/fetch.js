import test from 'blue-tape'

import { fetch } from '../../source/utils'
import agent from '../mock/agent'

test('fetch: default', t => {
  t.plan(0)

  // const getResources = () => ({
  //   testResource: '/test',
  // })
  //
  // const expectedResult = {
  //   uri: '/test',
  //   operationName: 'list',
  // }
  //
  // return fetch({params: {}, getResources, agent})
  //   .then(result => {
  //
  //     t.deepEqual(result, expectedResult)
  //
  //     t.end()
  //   })
  t.end()

})
